Installation of FastAPI

1. create a virtual environment:
 
    -> python -m venv myenv

2. install FastAPI and webserver uvicorn

   -> pip install fastAPI and uvicorn
   
3. mian Imports

 -> from pydantic import BaseModel

 
